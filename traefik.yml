# Traefik Static Configuration
# Entrypoints and providers are defined here.

entryPoints:
  web:
    address: ":80"
  traefik:
    address: ":8080"

api:
  dashboard: true

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true